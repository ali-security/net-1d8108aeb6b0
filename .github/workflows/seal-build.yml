name: seal-build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build module zip helper
        run: |
          set -euo pipefail
          HELPER="$(mktemp -d)"
          cp "$GITHUB_WORKSPACE/.github/create_mod.go" "$HELPER/create_mod.go"
          cd "$HELPER"
          go mod init example.com/mod
          go get golang.org/x/mod@v0.24.0
          go build -o "$HELPER/create_mod" ./create_mod.go
          echo "HELPER=$HELPER" >> "$GITHUB_ENV"

      - name: Package module zip
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/out"
          # x/net ships NO .github dir in its published zip; remove ours so it
          # doesn't leak into the module zip (CreateFromDir descends into .github).
          rm -rf "$GITHUB_WORKSPACE/.github"
          "$HELPER/create_mod" golang.org/x/net v0.45.0 "$GITHUB_WORKSPACE" "$RUNNER_TEMP/out/v0.45.0.zip"
          ls -l "$RUNNER_TEMP/out"

      - name: Upload module zip
        uses: actions/upload-artifact@v4
        with:
          name: module-zip
          path: ${{ runner.temp }}/out/v0.45.0.zip
          if-no-files-found: error

      - name: Run tests
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          go build ./...
          go test ./...
